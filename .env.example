# ============================================
# Ferrite Environment Configuration
# ============================================
# SECURITY WARNING: Never commit the actual .env file!
# Copy this file to .env and fill in your secure values.
# ============================================

# ============================================
# PostgreSQL Database Configuration
# ============================================
# REQUIRED: Database credentials must be set
# Use strong passwords for production environments
POSTGRES_USER=your_db_username
POSTGRES_PASSWORD=your_secure_db_password_here
POSTGRES_DB=ferrite
POSTGRES_PORT=5432

# ============================================
# Application Configuration
# ============================================
# REQUIRED: JWT secret for token signing
# MUST be at least 32 characters long
# Generate with: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_minimum_32_characters_required_please_change_this

# Server settings
HOST=0.0.0.0
PORT=8080
APP_PORT=8080

# ============================================
# Logging Configuration
# ============================================
RUST_LOG=info,ferrite=debug
RUST_BACKTRACE=1

# ============================================
# PgAdmin Configuration (Optional)
# ============================================
# Only needed if using docker-compose --profile tools
PGADMIN_EMAIL=admin@ferrite.local
PGADMIN_PASSWORD=your_pgadmin_password_here
PGADMIN_PORT=5050

# ============================================
# Security Notes:
# ============================================
# 1. Generate strong passwords using: pwgen -s 32 1
# 2. Generate JWT secret using: openssl rand -base64 32
# 3. Never use default or example values in production
# 4. Rotate secrets regularly
# 5. Keep .env file permissions restricted: chmod 600 .env
# ============================================
